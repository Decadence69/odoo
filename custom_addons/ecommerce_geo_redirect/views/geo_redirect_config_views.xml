<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_geo_redirect_config_tree" model="ir.ui.view">
        <field name="name">geo.redirect.config.list</field>
        <field name="model">geo.redirect.config</field>
        <field name="arch" type="xml">
            <list string="Geographic Redirects">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="country_code"/>
                <field name="redirect_url"/>
                <field name="active"/>
            </list>
        </field>
    </record>

    <record id="view_geo_redirect_config_form" model="ir.ui.view">
        <field name="name">geo.redirect.config.form</field>
        <field name="model">geo.redirect.config</field>
        <field name="arch" type="xml">
            <form string="Geographic Redirect">
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="toggle_active" type="object"
                                class="oe_stat_button" icon="fa-archive">
                            <field name="active" widget="boolean_button"
                                options='{"terminology": "archive"}'/>
                        </button>
                    </div>
                    <group>
                        <group>
                            <field name="name"/>
                            <field name="country_code"/>
                        </group>
                        <group>
                            <field name="redirect_url" placeholder="http://th.mrbur.shop"/>
                            <field name="sequence"/>
                        </group>
                    </group>
                </sheet>
            </form>
        </field>
    </record>

    <record id="action_geo_redirect_config" model="ir.actions.act_window">
        <field name="name">Redirect Rules</field>
        <field name="res_model">geo.redirect.config</field>
        <field name="view_mode">list,form</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                Create your first geographic redirect rule
            </p>
            <p>
                Configure country-specific redirects to automatically send users to their local website version.
            </p>
        </field>
    </record>

    <!-- Parent Menu (non-clickable container) -->
    <menuitem id="menu_geo_redirect_root"
              name="Geographic Redirects"
              parent="website.menu_website_configuration"
              sequence="50"/>
    
    <!-- Child Menu Items -->
    <menuitem id="menu_geo_redirect_config"
              name="Redirect Rules"
              parent="menu_geo_redirect_root"
              action="action_geo_redirect_config"
              sequence="10"/>
</odoo>