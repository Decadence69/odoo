<odoo>
    <!-- Form View for Salt Edge Settings -->
    <!-- Inject into General Settings -->
    <record id="view_salt_edge_settings_form" model="ir.ui.view">
        <field name="name">salt.edge.settings.form</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="base.view_res_config_settings"/>
        <field name="arch" type="xml">
            <xpath expr="//div[@id='settings']" position="inside">
                <div class="app_settings_block" data-string="Salt Edge" string="Salt Edge">
                    <h2>Salt Edge Configuration</h2>
                    <div class="row mt16 o_settings_container">
                        <div class="col-12 col-lg-6 o_setting_box">
                            <div class="o_setting_right_pane">
                                <field name="app_id"/>
                                <field name="secret"/>
                            </div>
                        </div>
                    </div>
                </div>
            </xpath>
        </field>
    </record>
    
    <!-- Action to open the settings form -->
    <record id="action_salt_edge_settings" model="ir.actions.act_window">
        <field name="name">Salt Edge Settings</field>
        <field name="res_model">salt.edge.settings</field>
        <field name="view_mode">form</field>
        <field name="target">current</field>
        <!-- Optional: restrict to one record if it's a config singleton -->
        <field name="view_id" ref="view_salt_edge_settings_form"/>
    </record>

    <!-- Main Menu -->
    <menuitem id="menu_salt_edge_root" name="Salt Edge" sequence="100"/>

    <!-- Submenu + Action -->
    <menuitem id="menu_salt_edge_config"
              name="Configuration"
              parent="menu_salt_edge_root"
              action="action_salt_edge_settings"
              sequence="10"/>
</odoo>
